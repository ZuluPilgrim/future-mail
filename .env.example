# FutureMail — .env is entirely optional.
#
# The JWT secret is auto-generated and saved to secret.key on first run.
# SMTP settings are configured via the Admin → Email Settings page.
#
# The only reason to create this file is if you want to run on a non-default port:
#
# PORT=3001
